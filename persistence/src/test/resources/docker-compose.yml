version: "2.0"
services:
  postgres:
    image: "postgres:10.5"
    environment:
      - POSTGRES_DB=adlershof
      - POSTGRES_USER=adlershof
      - POSTGRES_PASSWORD=secret
    ports:
      - 5432:5432